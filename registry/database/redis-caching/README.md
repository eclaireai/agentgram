# Redis Caching Layer

> Response caching with Redis — automatic invalidation, TTL, and cache-aside pattern

## What it does

Add Redis caching to any Express API using the cache-aside pattern. Includes a cacheMiddleware for automatic route-level caching, manual cache invalidation helpers, and graceful degradation if Redis is down.

## Why this recipe exists

Your database is fast. Redis is 100x faster. For read-heavy endpoints (product listings, user profiles, search results), caching at the route level drops p99 latency from 200ms to 2ms — without changing a line of business logic.

## Steps (9 total)

1. 🔍 **`find`** → `package.json`  
   Check existing dependencies and Redis setup

2. ⚡ **`run_command`** → `npm install ioredis`  
   Install ioredis client (better than node-redis for TypeScript)

3. 📄 **`create_file`** → `src/lib/redis.ts`  
   Create Redis client singleton with connection error handling and graceful fallback

4. 📄 **`create_file`** → `src/middleware/cache.ts`  
   Create cache(ttlSeconds) middleware using cache-aside pattern with req.path as key

5. 📄 **`create_file`** → `src/utils/invalidate.ts`  
   Create invalidateCache(pattern) helper for manual cache busting

6. ✏️ **`modify_file`** → `.env`  
   Add REDIS_URL environment variable

7. ✏️ **`modify_file`** → `src/routes/products.ts`  
   Apply cache(300) middleware to GET /products and GET /products/:id

8. ✏️ **`modify_file`** → `src/routes/products.ts`  
   Call invalidateCache on POST/PUT/DELETE mutations

9. ⚡ **`run_command`** → `npm test`  
   Verify cache hit/miss behavior in tests

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `src` | `src` | Change to match your project |

## Tags

`redis` · `caching` · `performance` · `express` · `nodejs` · `typescript`

## Stack

Works with: nodejs, express, typescript

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
