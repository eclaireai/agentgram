# Mongoose + MongoDB

> Document database with schema validation, indexes, and connection management

## What it does

Set up Mongoose with MongoDB Atlas or local Docker. Includes schema validation, compound indexes, connection retry logic, and TypeScript interfaces auto-generated from schemas.

## Why this recipe exists

MongoDB without Mongoose is schema anarchy. Mongoose gives you validation, middleware hooks, and indexes — while keeping MongoDB's flexibility. This recipe sets it up with connection resilience for production.

## Steps (10 total)

1. 🔍 **`find`** → `package.json`  
   Check existing dependencies

2. ⚡ **`run_command`** → `npm install mongoose`  
   Install Mongoose

3. ⚡ **`run_command`** → `npm install -D @types/mongoose`  
   Install Mongoose type definitions

4. 📄 **`create_file`** → `src/lib/mongodb.ts`  
   Create connection helper with retry logic and connection state caching

5. 📄 **`create_file`** → `src/models/User.ts`  
   Create User schema with name, email (unique index), password, createdAt, and toJSON transform

6. 📄 **`create_file`** → `src/models/index.ts`  
   Export all models from single entry point

7. ✏️ **`modify_file`** → `.env`  
   Add MONGODB_URI environment variable

8. ✏️ **`modify_file`** → `src/app.ts`  
   Connect to MongoDB on startup with error handling

9. 📄 **`create_file`** → `tests/user.test.ts`  
   Add tests using mongodb-memory-server for in-memory testing

10. ⚡ **`run_command`** → `npm test`  
   Verify Mongoose tests pass with in-memory server

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `src` | `src` | Change to match your project |
| `tests` | `tests` | Change to match your project |

## Tags

`database` · `mongodb` · `mongoose` · `nodejs` · `typescript`

## Stack

Works with: nodejs, typescript

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
