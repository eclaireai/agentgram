# Structured Logging with Pino

> JSON logs with request IDs, levels, and millisecond timestamps — ready for Datadog/CloudWatch

## What it does

Replace console.log with Pino structured logging. Every log line is JSON with timestamp, level, requestId, and context. Log levels are configurable per environment. Integrates with any log aggregator.

## Why this recipe exists

console.log is invisible in production. Pino produces JSON logs that Datadog, CloudWatch, and Splunk can parse, filter, and alert on. Request IDs let you trace a single request across all log lines.

## Steps (8 total)

1. 🔍 **`find`** → `src/app.ts`  
   Check Express setup and existing logging

2. ⚡ **`run_command`** → `npm install pino pino-http`  
   Install Pino and HTTP middleware

3. ⚡ **`run_command`** → `npm install -D pino-pretty`  
   Install pretty printer for local development

4. 📄 **`create_file`** → `src/lib/logger.ts`  
   Create Pino logger singleton with level from LOG_LEVEL env, pretty transport in dev

5. 📄 **`create_file`** → `src/middleware/requestLogger.ts`  
   Create pino-http middleware that adds requestId to every request and logs method/url/statusCode/responseTime

6. ✏️ **`modify_file`** → `src/app.ts`  
   Add requestLogger middleware as first middleware after Helmet

7. ✏️ **`modify_file`** → `src/**/*.ts`  
   Replace console.log calls with logger.info/warn/error with structured context objects

8. ⚡ **`run_command`** → `npm start 2>&1 | head -5`  
   Verify logs are JSON in production format

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `src` | `src` | Change to match your project |

## Tags

`logging` · `pino` · `monitoring` · `observability` · `nodejs` · `typescript`

## Stack

Works with: nodejs, typescript

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
