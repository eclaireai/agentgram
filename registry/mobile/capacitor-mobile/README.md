# Web to iOS/Android with Capacitor

> Ship your existing web app as a native iOS and Android app today

## Why This Exists

If you already have a web app, Capacitor is the fastest path to the App Store. It's not a WebView hack — it's the same approach Ionic uses, with direct access to 100+ native APIs. Your web developers become mobile developers.

## Steps (7 total)

1. ▶️ **Install Capacitor core and CLI**
   `npm install @capacitor/core @capacitor/cli`

2. ▶️ **Initialize Capacitor in your web project**
   `npx cap init "My App" "com.company.myapp"`

3. ▶️ **Add iOS and Android platforms**
   `npm install @capacitor/ios @capacitor/android`

4. ▶️ **Add native plugins for camera, GPS, and push**
   `npm install @capacitor/camera @capacitor/geolocation @capacitor/push-notifications`

5. ▶️ **Build web app and sync to native projects**
   `npm run build && npx cap sync`

6. ▶️ **Open in Xcode for iOS build and App Store submission**
   `npx cap open ios`

7. ▶️ **Open in Android Studio for Google Play submission**
   `npx cap open android`

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `APP_ID` | `com.company.myapp` | — |
| `WEB_DIR` | `out` | — |
## Tags

`mobile` · `capacitor` · `ios` · `android` · `native` · `cross-platform`

## Stack

Works with: nextjs, nodejs

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
