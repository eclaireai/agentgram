# React Native with Expo EAS

> Build iOS and Android from one codebase — OTA updates, no App Store wait

## Why This Exists

React Native without Expo means maintaining Xcode, Android Studio, signing certificates, and build scripts. Expo handles all of it. EAS lets you push JavaScript updates OTA — your users get fixes in minutes, not 2 days of App Store review.

## Steps (7 total)

1. ▶️ **Create new Expo app with TypeScript template**
   `npx create-expo-app@latest my-app --template blank-typescript`

2. ▶️ **Install EAS CLI and authenticate**
   `npm install -g eas-cli && eas login`

3. ▶️ **Configure EAS builds — creates eas.json with development/preview/production profiles**
   `eas build:configure`

4. ▶️ **Configure OTA updates — creates update branch per EAS profile**
   `eas update:configure`

5. 📄 **Set up Expo Router with tab and stack navigation, typed routes**
   `app/_layout.tsx`

6. ▶️ **Build development client for iOS and Android**
   `eas build --profile development --platform all`
   *Expected: Build submitted*

7. ▶️ **Push first OTA update to preview channel**
   `eas update --branch preview --message "First update"`

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `APP_SLUG` | `my-app` | — |
| `BUNDLE_ID` | `com.company.myapp` | — |
## Tags

`mobile` · `expo` · `react-native` · `ios` · `android` · `ota` · `eas`

## Stack

Works with: react-native

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
