# Request Validation with Zod

> Type-safe request parsing — runtime validation from your TypeScript types

## What it does

Add Zod schema validation to all API endpoints. Request bodies, query params, and route params are validated at the boundary — invalid requests get a 400 with detailed error messages before touching business logic.

## Why this recipe exists

TypeScript catches type errors at compile time. Zod catches them at runtime — from untrusted user input. Without Zod, you're trusting that req.body matches your types. With Zod, you know it does.

## Steps (8 total)

1. 🔍 **`find`** → `package.json`  
   Check Express version and existing validation setup

2. ⚡ **`run_command`** → `npm install zod`  
   Install Zod

3. 📄 **`create_file`** → `src/middleware/validate.ts`  
   Create validate(schema) middleware that parses req.body with Zod and returns 400 on failure with ZodError details

4. 📄 **`create_file`** → `src/schemas/user.ts`  
   Define createUserSchema, updateUserSchema, and loginSchema with Zod

5. ✏️ **`modify_file`** → `src/routes/users.ts`  
   Apply validate(createUserSchema) to POST /users, validate(updateUserSchema) to PUT /users/:id

6. ✏️ **`modify_file`** → `src/routes/auth.ts`  
   Apply validate(loginSchema) to POST /auth/login

7. 📄 **`create_file`** → `tests/validation.test.ts`  
   Test that invalid payloads return 400 with field-level errors

8. ⚡ **`run_command`** → `npm test`  
   Verify validation tests pass

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `src` | `src` | Change to match your project |
| `tests` | `tests` | Change to match your project |

## Tags

`validation` · `zod` · `api` · `typescript` · `express` · `nodejs`

## Stack

Works with: nodejs, express, typescript

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
