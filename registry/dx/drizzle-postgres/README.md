# Drizzle ORM + PostgreSQL

> Type-safe SQL that feels like writing SQL — migrations, relations, zero runtime overhead

## Why This Exists

Prisma is a black box — it generates a client you can't fully control, with runtime overhead. Drizzle is transparent SQL in TypeScript. No magic, no hidden N+1, and it's the fastest ORM for Node.js by benchmark.

## Steps (8 total)

1. ▶️ **Install Drizzle ORM and postgres client**
   `npm install drizzle-orm postgres`

2. ▶️ **Install Drizzle Kit for migrations**
   `npm install -D drizzle-kit`

3. 📄 **Define tables using pgTable(), column types, and indexes**
   `db/schema/index.ts`

4. 📄 **Create Drizzle client with postgres connection pool, exported as db**
   `db/index.ts`

5. 📄 **Configure schema path, migrations output, and connection string for Drizzle Kit**
   `drizzle.config.ts`

6. ▶️ **Generate SQL migration from schema diff**
   `npx drizzle-kit generate`

7. ▶️ **Apply migration to database**
   `npx drizzle-kit migrate`
   *Expected: Migrations applied*

8. ▶️ **Open Drizzle Studio at https://local.drizzle.studio**
   `npx drizzle-kit studio`

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `DB_URL` | `postgresql://user:password@localhost/mydb` | — |
## Tags

`dx` · `drizzle` · `orm` · `postgresql` · `typescript` · `sql` · `migrations`

## Stack

Works with: nextjs, nodejs

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
