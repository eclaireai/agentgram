# shadcn/ui + Tailwind v4

> The component library that owns no code — beautiful, accessible, yours to modify

## Why This Exists

shadcn/ui copies components into your codebase — you own the code, you modify it freely, no fighting with library overrides. It's the UI kit that doesn't fight you. Combined with Tailwind v4's native cascade layers, this is the stack for 2026.

## Steps (6 total)

1. ▶️ **Initialize shadcn/ui (selects style, base color, CSS variables)**
   `npx shadcn@latest init`

2. ▶️ **Add the 10 most-used components**
   `npx shadcn@latest add button dialog sheet form table card badge toast`

3. 📄 **Configure Tailwind v4 @theme block with custom colors, fonts, and spacing**
   `app/globals.css`

4. 📄 **Create dark/light mode toggle using next-themes**
   `components/theme-toggle.tsx`

5. ▶️ **Add Storybook for component development**
   `npm install -D @storybook/nextjs`

6. ▶️ **Initialize Storybook with Next.js framework**
   `npx storybook@latest init`

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `BASE_COLOR` | `slate` | — |
| `STYLE` | `default` | — |
## Tags

`dx` · `shadcn` · `tailwind` · `ui` · `components` · `dark-mode` · `accessibility`

## Stack

Works with: nextjs

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
