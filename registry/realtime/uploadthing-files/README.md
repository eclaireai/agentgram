# File Uploads with UploadThing

> Type-safe file uploads with progress tracking, validation, and CDN delivery

## Why This Exists

Building file uploads correctly (presigned URLs, progress tracking, CDN, virus scanning) takes days. UploadThing wraps it in 20 lines with TypeScript end-to-end. Your users get progress bars; you get validated, CDN-served files.

## Steps (6 total)

1. ▶️ **Install UploadThing and React components**
   `npm install uploadthing @uploadthing/react`

2. 📄 **Create file router with routes (imageUploader, documentUploader), auth check, file type/size rules, onUploadComplete callback**
   `app/api/uploadthing/core.ts`

3. 📄 **Create Next.js route handler from your file router**
   `app/api/uploadthing/route.ts`

4. 📄 **Export typed client components: UploadButton, UploadDropzone, Uploader**
   `lib/uploadthing.ts`

5. 📄 **Create file upload component with drag-and-drop, progress bar, and preview**
   `components/FileUpload.tsx`

6. ✏️ **Add UPLOADTHING_SECRET and UPLOADTHING_APP_ID from UploadThing dashboard**
   `.env.local`

## Parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `MAX_FILE_SIZE` | `4MB` | — |
| `ALLOWED_TYPES` | `image/*, application/pdf` | — |
## Tags

`uploads` · `uploadthing` · `files` · `cdn` · `s3` · `nextjs`

## Stack

Works with: nextjs

---

*Generated by [agentgram](https://github.com/eclaireai/agentgram) — the AI agent memory layer*
